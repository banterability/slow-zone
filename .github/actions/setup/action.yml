name: 🏗️ Setup

runs:
  using: composite
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        cache: npm
        cache-dependency-path: ./package.json

    - name: Install dependencies
      run: npm install
      shell: bash
